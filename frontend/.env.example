# Copy this file to .env and adjust values for your environment
# The Vite proxy (vite.config.ts) handles /api/* -> backend, so no VITE_API_URL needed in dev.
# In production builds, set this to your deployed API origin.
VITE_API_BASE_URL=http://localhost:5001

